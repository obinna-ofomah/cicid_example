name: My First Workflow

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
    - name: Welcome Mwssage
      run: echo "Welcome, Obinna"

    - name: List Files
      run: ls

    - name: Read File
      run: cat README.md